<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de senha</title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Arial, Helvetica, sans-serif;
    }

    body {
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    h1 {
        margin-top: 15px;
    }

    .container {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        border-radius: 10px;
        width: 500px;
        box-shadow: 1px 2px 15px -5px rgba(0,0,0,0.69);
    }

    input#email.form-control, input#senhaDigitada.form-control {
        width: 460px;
    }

    .button-container {
        width: 100%; /* Ocupa toda a largura disponível */
        display: flex; /* Usa flexbox */
        justify-content: space-between; /* Espaça o botão e o link */
        margin-top: 20px; /* Espaço acima do botão */
    }

    button.btn.btn-primary {
        width: auto; /* Ajusta a largura do botão */
        margin-bottom: 15px;
    }

    a {
        align-self: center; /* Centraliza o link verticalmente */
        text-align: right; /* Alinha o texto à direita */
    }

    a.btn.btn-secondary {
        align-self: center; /* Centraliza o link verticalmente */
        text-align: right; /* Alinha o texto à direita */
        margin-bottom: 28px;
    }
</style>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Cadastro de senhas</h1>
        <form action="/senhas/save" method="POST">
            <div class="mb-3">
                <label for="nome" class="form-label">Nome:</label>
                <input type="text" id="nome" name="nome" class="form-control" placeholder="Nome:" required>
            </div>
            <div class="mb-3">
                <label for="valor" class="form-label">Valor:</label>
                <input type="text" id="valor" name="valor" class="form-control" placeholder="Senha:" required>                
            </div>
            <div class="mb-3">
                <button type="submit" class="btn btn-primary">Salvar</button>
                <a href="/senha" class="btn btn-secondary mt-3">Ver Lista de senhas</a>
            </div>           
        </form>        
    </div>
    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>